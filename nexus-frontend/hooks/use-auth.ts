/**
 * useAuth Hook
 * 
 * Re-exports the useAuth hook from auth-context for convenience
 * This allows importing from hooks/ directory following the project structure
 */

export { useAuth } from '@/contexts/auth-context'
